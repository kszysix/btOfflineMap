# shared/protocol

Placeholder — this directory will hold the versioned Garmin-Android BLE protocol definition
(`docs/PROJECT_PLAN.md` §33), starting with `HELLO`/`HELLO_ACK`/`GPS_UPDATE` in Phase 1.

Nothing is defined here yet. `docs/protocol.md` will document the protocol once it exists;
it isn't created yet either, to avoid an empty doc with nothing to say (§74).
