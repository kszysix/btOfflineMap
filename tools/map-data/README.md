# tools/map-data

Placeholder — this directory will hold the OSM `.osm.pbf` -> PMTiles preprocessing pipeline
(`docs/PROJECT_PLAN.md` §13), needed starting Phase 4.

Nothing is here yet; Phase 0–3 don't touch map data at all (§60: communication, GPS,
activity, and track come before any map work).
